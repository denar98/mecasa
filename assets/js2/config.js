var primary = localStorage.getItem("primary") || '#073D5E';
var secondary = localStorage.getItem("secondary") || '#F05B3B';

window.CubaAdminConfig = {
	// Theme Primary Color
	primary: primary,
	// theme secondary color
	secondary: secondary,
};
